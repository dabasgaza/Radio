<UserControl
    x:Class="Radio.Controls.SkeletonLoader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  فرشاة التدرج للوميض  -->
        <LinearGradientBrush x:Key="ShimmerBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Offset="-1" Color="Transparent" />
            <GradientStop Offset="0" Color="#14FFFFFF" />
            <GradientStop Offset="1" Color="Transparent" />
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid>
        <!--  محتوى الهيكل العظمي (Skeleton)  -->
        <Grid Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  ✨ سطر العنوان  -->
            <StackPanel Grid.Row="0" Margin="28,24,28,0">
                <Border
                    Width="200"
                    Height="28"
                    HorizontalAlignment="Right"
                    Background="{DynamicResource SurfaceVariantBrush}"
                    CornerRadius="8" />
                <Border
                    Width="350"
                    Height="14"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Right"
                    Background="{DynamicResource DividerBrush}"
                    CornerRadius="4" />
            </StackPanel>

            <!--  ✨ بطاقات الإحصائيات  -->
            <Grid Grid.Row="1" Margin="28,20,28,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Height="100"
                    Margin="6,0"
                    Background="{DynamicResource SurfaceVariantBrush}"
                    CornerRadius="16" />
                <Border
                    Grid.Column="1"
                    Height="100"
                    Margin="6,0"
                    Background="{DynamicResource SurfaceVariantBrush}"
                    CornerRadius="16" />
                <Border
                    Grid.Column="2"
                    Height="100"
                    Margin="6,0"
                    Background="{DynamicResource SurfaceVariantBrush}"
                    CornerRadius="16" />
                <Border
                    Grid.Column="3"
                    Height="100"
                    Margin="6,0"
                    Background="{DynamicResource SurfaceVariantBrush}"
                    CornerRadius="16" />
            </Grid>

            <!--  ✨ خطوط المحتوى  -->
            <StackPanel Grid.Row="2" Margin="28,24,28,0">
                <Border
                    Width="180"
                    Height="18"
                    HorizontalAlignment="Right"
                    Background="{DynamicResource SurfaceVariantBrush}"
                    CornerRadius="4" />
            </StackPanel>

            <!--  ✨ الجدول  -->
            <Border
                Grid.Row="3"
                Margin="28,16,28,24"
                Background="{DynamicResource SurfaceVariantBrush}"
                CornerRadius="16" />
        </Grid>

        <!--  المحتوى الحقيقي  -->
        <ContentPresenter Content="{Binding ChildContent, RelativeSource={RelativeSource AncestorType=UserControl}}" Visibility="Collapsed" />
    </Grid>
</UserControl>
