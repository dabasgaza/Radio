<mah:MetroWindow
    x:Class="Radio.Views.Users.UserFormDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="إدارة بيانات المستخدم"
    Width="650"
    Height="750"
    AllowsTransparency="True"
    Background="Transparent"
    FlowDirection="RightToLeft"
    ShowTitleBar="False"
    Style="{StaticResource BasicWindowStyle}"
    UseNoneWindowStyle="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">

    <materialDesign:Card
        Margin="30"
        materialDesign:ElevationAssist.Elevation="Dp2"
        Background="{DynamicResource MaterialDesignPaper}"
        UniformCornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <materialDesign:ColorZone
                Padding="30,25"
                Foreground="White"
                Mode="Custom">
                <materialDesign:ColorZone.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Offset="0" Color="#4527A0" />
                        <GradientStop Offset="1" Color="#0097A7" />
                    </LinearGradientBrush>
                </materialDesign:ColorZone.Background>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Width="60"
                        Height="60"
                        Background="#30FFFFFF"
                        CornerRadius="30">
                        <materialDesign:PackIcon
                            Width="35"
                            Height="35"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Kind="AccountTie" />
                    </Border>

                    <StackPanel
                        Grid.Column="1"
                        Margin="20,0"
                        VerticalAlignment="Center">
                        <TextBlock
                            x:Name="TxtTitle"
                            FontSize="24"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="إدارة بيانات المستخدم" />
                        <TextBlock
                            x:Name="TxtSubtitle"
                            FontSize="12"
                            Foreground="White"
                            Opacity="0.8"
                            Text="يرجى ملء كافة الحقول لضمان أمن الحساب" />
                    </StackPanel>

                    <Button
                        Grid.Column="2"
                        VerticalAlignment="Top"
                        Click="BtnCancel_Click"
                        Foreground="White"
                        Opacity="0.5"
                        Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon
                            Width="24"
                            Height="24"
                            Kind="Close" />
                    </Button>
                </Grid>
            </materialDesign:ColorZone>

            <ScrollViewer
                Grid.Row="1"
                Padding="35,25"
                VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10,15">

                    <StackPanel Margin="0,0,0,15" Orientation="Horizontal">
                        <materialDesign:PackIcon
                            VerticalAlignment="Center"
                            Foreground="#4527A0"
                            Kind="ShieldAccount" />
                        <TextBlock
                            Margin="10,0"
                            FontSize="12"
                            FontWeight="Bold"
                            Opacity="0.6"
                            Text="بيانات الحساب والصلاحيات" />
                    </StackPanel>

                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="20" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBox
                            x:Name="TxtFullName"
                            Grid.Row="0"
                            Grid.Column="0"
                            materialDesign:HintAssist.Hint="الاسم الكامل *"
                            materialDesign:TextFieldAssist.HasLeadingIcon="True"
                            materialDesign:TextFieldAssist.LeadingIcon="AccountCard"
                            Style="{StaticResource BroadcastTextBox}" />

                        <TextBox
                            x:Name="TxtUsername"
                            Grid.Row="0"
                            Grid.Column="2"
                            materialDesign:HintAssist.Hint="اسم المستخدم *"
                            materialDesign:TextFieldAssist.HasLeadingIcon="True"
                            materialDesign:TextFieldAssist.LeadingIcon="At"
                            Style="{StaticResource BroadcastTextBox}" />

                        <PasswordBox
                            x:Name="TxtPassword"
                            Grid.Row="2"
                            Grid.Column="0"
                            Height="52"
                            materialDesign:HintAssist.Hint="كلمة المرور"
                            materialDesign:TextFieldAssist.HasLeadingIcon="True"
                            materialDesign:TextFieldAssist.LeadingIcon="LockOutline"
                            Style="{StaticResource MaterialDesignOutlinedPasswordBox}" />

                        <ComboBox
                            x:Name="CboRoles"
                            Grid.Row="2"
                            Grid.Column="2"
                            materialDesign:HintAssist.Hint="الدور / الصلاحية *"
                            materialDesign:TextFieldAssist.HasLeadingIcon="True"
                            materialDesign:TextFieldAssist.LeadingIcon="ShieldKeyOutline"
                            DisplayMemberPath="RoleName"
                            SelectedValuePath="RoleId"
                            Style="{StaticResource BroadcastComboBox}" />
                    </Grid>

                    <Border
                        x:Name="PwdHintCard"
                        Margin="0,-10,0,30"
                        Padding="15,10"
                        Background="#104527A0"
                        CornerRadius="10"
                        Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon
                                Width="16"
                                Foreground="#4527A0"
                                Kind="Information" />
                            <TextBlock
                                x:Name="TxtPwdHint"
                                Margin="10,0"
                                VerticalAlignment="Center"
                                FontSize="11"
                                Foreground="#4527A0"
                                Text="اترك كلمة المرور فارغة إذا كنت لا تريد تغييرها" />
                        </StackPanel>
                    </Border>

                    <Separator Margin="0,0,0,30" Background="{DynamicResource MaterialDesignDivider}" />

                    <StackPanel Margin="0,0,0,15" Orientation="Horizontal">
                        <materialDesign:PackIcon
                            VerticalAlignment="Center"
                            Foreground="#0097A7"
                            Kind="ContactPhone" />
                        <TextBlock
                            Margin="10,0"
                            FontSize="12"
                            FontWeight="Bold"
                            Opacity="0.6"
                            Text="معلومات التواصل والاتصال" />
                    </StackPanel>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBox
                            x:Name="TxtPhone"
                            Grid.Column="0"
                            materialDesign:HintAssist.Hint="رقم الهاتف"
                            materialDesign:TextFieldAssist.HasLeadingIcon="True"
                            materialDesign:TextFieldAssist.LeadingIcon="PhoneOutline"
                            Style="{StaticResource BroadcastTextBox}" />

                        <TextBox
                            x:Name="TxtEmail"
                            Grid.Column="2"
                            materialDesign:HintAssist.Hint="البريد الإلكتروني"
                            materialDesign:TextFieldAssist.HasLeadingIcon="True"
                            materialDesign:TextFieldAssist.LeadingIcon="EmailOutline"
                            Style="{StaticResource BroadcastTextBox}" />
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <Border
                Grid.Row="2"
                Padding="35,25"
                Background="{DynamicResource MaterialDesignDivider}"
                CornerRadius="0,0,20,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="1.5*" />
                    </Grid.ColumnDefinitions>

                    <Button
                        x:Name="BtnCancel"
                        Grid.Column="0"
                        Click="BtnCancel_Click"
                        Content="إلغاء التغييرات"
                        Style="{StaticResource CancelFlatButton}" />

                    <Button
                        x:Name="BtnSave"
                        Grid.Column="2"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        materialDesign:ElevationAssist.Elevation="Dp2"
                        Background="#4527A0"
                        BorderBrush="#4527A0"
                        Click="BtnSave_Click"
                        Content="حفظ بيانات الحساب"
                        Style="{StaticResource PrimaryActionButton}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon VerticalAlignment="Center" Kind="AccountCheckOutline" />
                                    <TextBlock
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        FontWeight="Bold"
                                        Text="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </materialDesign:Card>
</mah:MetroWindow>