<mah:MetroWindow
    x:Class="Radio.Views.Users.PermissionMatrixView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="إدارة صلاحيات الوصول المتقدمة"
    Width="1000"
    Height="650"
    Style="{StaticResource Window.Dialog}"
    WindowStartupLocation="CenterScreen">

    <materialDesign:Card
        Margin="30"
        materialDesign:ElevationAssist.Elevation="Dp3"
        Background="{DynamicResource MaterialDesignPaper}"
        UniformCornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <materialDesign:ColorZone MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Style="{StaticResource Zone.Header.Primary}">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <materialDesign:PackIcon
                        Width="40"
                        Height="40"
                        VerticalAlignment="Center"
                        Kind="ShieldLockOutline" />

                    <StackPanel Grid.Column="1" Margin="20,0">
                        <TextBlock Style="{StaticResource Type.TitleLarge.OnColor}" Text="مصفوفة الصلاحيات السيادية" />
                        <TextBlock Style="{StaticResource Type.BodyMedium.OnColor}" Text="التحكم الكامل في أدوار المستخدمين وصلاحيات الوصول للوحدات البرمجية" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button
                            x:Name="BtnSavePermissions"
                            Height="45"
                            Padding="20,0"
                            materialDesign:ButtonAssist.CornerRadius="10"
                            Background="#FFB300"
                            BorderThickness="0"
                            Click="BtnSavePermissions_Click"
                            Foreground="#1B5E20">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon VerticalAlignment="Center" Kind="ShieldCheck" />
                                <TextBlock
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    Text="اعتماد وحفظ التغييرات" />
                            </StackPanel>
                        </Button>

                        <Button
                            Margin="20,0,0,0"
                            Click="BtnClose_Click"
                            Foreground="White"
                            Opacity="0.5"
                            Style="{StaticResource MaterialDesignIconButton}">
                            <materialDesign:PackIcon
                                Width="24"
                                Height="24"
                                Kind="Close" />
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:ColorZone>

            <Grid Grid.Row="1" Margin="25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" />
                    <ColumnDefinition Width="25" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <materialDesign:Card
                    Grid.Column="0"
                    materialDesign:ElevationAssist.Elevation="Dp1"
                    UniformCornerRadius="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Margin="5" Style="{StaticResource Card.Info}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountKey" />
                                <TextBlock
                                    Margin="12,0"
                                    Foreground="{StaticResource PrimaryLightBrush}"
                                    Text="الأدوار والوظائف" />
                            </StackPanel>
                        </Border>

                        <ListBox
                            x:Name="LstRoles"
                            Grid.Row="1"
                            Margin="5"
                            SelectionChanged="LstRoles_SelectionChanged"
                            Style="{StaticResource List.Navigation}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,6" Orientation="Horizontal">
                                        <materialDesign:PackIcon
                                            Width="18"
                                            Height="18"
                                            VerticalAlignment="Center"
                                            Kind="BadgeAccountHorizontalOutline"
                                            Opacity="0.5" />
                                        <TextBlock
                                            Margin="15,0"
                                            VerticalAlignment="Center"
                                            FontSize="16"
                                            Text="{Binding RoleName}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card
                    Grid.Column="2"
                    materialDesign:ElevationAssist.Elevation="Dp1"
                    UniformCornerRadius="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Margin="5" Style="{StaticResource Card.Info}">
                            <TextBlock Foreground="{StaticResource PrimaryLightBrush}" Text="توزيع صلاحيات الدور المختار" />
                        </Border>

                        <ScrollViewer
                            Grid.Row="1"
                            Padding="20"
                            VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ItemsPermissions">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid VerticalAlignment="Top" Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border
                                            Margin="8"
                                            Padding="18"
                                            Background="Transparent"
                                            BorderBrush="#C8E6C9"
                                            BorderThickness="1"
                                            CornerRadius="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock
                                                        FontSize="15"
                                                        Foreground="{DynamicResource MaterialDesignBody}"
                                                        Text="{Binding DisplayName}" />
                                                    <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                                                        <materialDesign:PackIcon
                                                            Width="12"
                                                            Height="12"
                                                            Kind="FolderOpenOutline"
                                                            Opacity="0.4" />
                                                        <TextBlock
                                                            Margin="5,0"
                                                            FontSize="10"
                                                            Opacity="0.5"
                                                            Text="{Binding Module}" />
                                                    </StackPanel>
                                                </StackPanel>

                                                <ToggleButton
                                                    Grid.Column="1"
                                                    Foreground="#2E7D32"
                                                    IsChecked="{Binding IsAssigned, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                                    ToolTip="منح الصلاحية لهذا الدور" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </materialDesign:Card>
</mah:MetroWindow>