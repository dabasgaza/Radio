<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Gradient Brushes for Dynamic Episode Cards -->
    <LinearGradientBrush x:Key="EpisodeCardGradient1" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF6B8DD6" Offset="0"/>
        <GradientStop Color="#FF8E37CE" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="EpisodeCardGradient2" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF11998E" Offset="0"/>
        <GradientStop Color="#FF38EF7D" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="EpisodeCardGradient3" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FFF093FB" Offset="0"/>
        <GradientStop Color="#FFF5576C" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="EpisodeCardGradient4" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF4FACFE" Offset="0"/>
        <GradientStop Color="#FF00F2FE" Offset="1"/>
    </LinearGradientBrush>

    <!-- Modern Glass Effect Card Style -->
    <Style x:Key="ModernEpisodeCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.15" Color="#000000"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Animated Menu Item Style -->
    <Style x:Key="ModernMenuItemButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Height" Value="42"/>
        <Setter Property="Margin" Value="2,1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="10"
                            Padding="10,0">
                        <ContentPresenter HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryMainBrush}"/>
                            <Setter TargetName="border" Property="Opacity" Value="0.1"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryMainBrush}"/>
                            <Setter TargetName="border" Property="Opacity" Value="0.2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Popup Menu Style -->
    <Style x:Key="ModernPopupMenu" TargetType="materialDesign:PopupBox">
        <Setter Property="PlacementMode" Value="BottomAndAlignRightEdges"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="0"/>
    </Style>

    <!-- Status Indicator Styles -->
    <Style x:Key="StatusPulse" TargetType="Ellipse">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Height" Value="8"/>
        <Setter Property="Fill" Value="#FF4CAF50"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.6" Color="#FF4CAF50"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>